import {db} from "@/lib/prisma";
export default async function Bot(){const c=await db.company.findFirst();return <><div className="top"><div><h1>Bot / IA</h1><div className="sub">Configure como seu assistente conversa</div></div><span className="pill">IA pronta para conectar</span></div><form className="form"><label className="label">Nome do bot<input className="input" defaultValue={c?.botName||"Assistente"} /></label><label className="label">Mensagem inicial<textarea className="textarea" defaultValue={c?.welcome||""}/></label><label className="label">Prompt / personalidade<textarea className="textarea" defaultValue={c?.prompt||""}/></label><button className="btn">Salvar configuração</button><p className="sub">A gravação deste formulário será conectada ao endpoint de configuração do backend.</p></form></>}
