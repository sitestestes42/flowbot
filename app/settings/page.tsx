import {db} from "@/lib/prisma";
export default async function Settings(){const c=await db.company.findFirst();return <><div className="top"><div><h1>Configurações</h1><div className="sub">Dados da empresa</div></div></div><div className="form"><label className="label">Nome da empresa<input className="input" defaultValue={c?.name||"Minha empresa"}/></label><label className="label">WhatsApp<input className="input" defaultValue={c?.phone||""}/></label><button className="btn">Salvar</button><p className="sub">Para a primeira instalação, crie uma empresa no banco com Prisma Studio ou pelo seed.</p></div></>}
